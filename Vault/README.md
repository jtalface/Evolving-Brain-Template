# Vault

> Central credentials store.

- `Vault/.env` — the big bolt. **Gitignored. Never commit.**
- `Vault/.env.example` — committed template. No real values.
- `Vault/key-inventory.md` — human-readable catalog of what keys exist and what they're for. No values.

Workflows never hardcode secrets.
