# Evolving Brain — environment variables template
# Copy to .env and fill in real values. Never commit .env.

# Example: Supabase (for gbrain retrieval layer)
SUPABASE_URL=
SUPABASE_KEY=

# Example: OpenAI (if using OpenAI embeddings)
OPENAI_API_KEY=

# Add your own keys below
