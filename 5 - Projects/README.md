# Projects

> PARA projects: defined outcome, end date, active work.

Each project gets its own file using the compiled-truth + timeline dossier pattern (see AGENTS.md). Frontmatter includes a persistent `id` field that never changes even if the file is renamed.
